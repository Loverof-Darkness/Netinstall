#cloud-config
# Example only. Generate a real file with NetInstall's config renderer.
autoinstall:
  version: 1
  locale: en_US.UTF-8
  keyboard:
    layout: us
  identity:
    hostname: netinstall-example
    username: installer
    password: '$REPLACE_WITH_CRYPT_HASH'
  ssh:
    install-server: false
